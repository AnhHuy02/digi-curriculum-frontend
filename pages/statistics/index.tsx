import Typography from "@mui/material/Typography";
import Box from "@mui/material/Box";

export const StatisticsPage = () => {
  return (
    <Box>
      <Typography paragraph>Statistics Page</Typography>
    </Box>
  );
};

export default StatisticsPage;
