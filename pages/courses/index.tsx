import Typography from "@mui/material/Typography";
import Box from "@mui/material/Box";

export const CoursesPage = () => {
  return (
    <Box>
      <Typography paragraph>Courses Page</Typography>
    </Box>
  );
};

export default CoursesPage;
